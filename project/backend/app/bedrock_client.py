import boto3
import os

def ask_bedrock(question: str):
    # Placeholder for AWS Bedrock call
    # In production: use boto3.client('bedrock-runtime') and call model
    return f"[Mocked Bedrock Response] You asked: {question}"
